# pipi 插件测试报告

日期：2026-09-09（Asia/Shanghai）。结论：部分完成，当前组合未通过成品验收。
本轮未更换插件、清理生产配置或修改桥接代码，保留已有未提交成果。

## 基线

AstrBot 4.28.0；SpectreCore 2.1.13；LivingMemory 2.7.0-beta.1；表情包小偷 3.0.0。
微信群管理和 Akasha 桥接仍在消息链路中。真实发送仅尝试既定“记录”测试群。

## 已执行测试

| 项目 | 结果与边界 |
| --- | --- |
| 服务与桥接连接 | 运行中，WeFlow/OneBot/UIA 标志正常，仅基础状态通过。 |
| 主模型、压缩模型 | 真实 API 返回正确标记；自动压缩触发未测。 |
| 两个备用模型 | 两轮聊天调用均返回上游 HTTP 502；模型目录中存在对应 ID。自动切换未通过验收。 |
| 视觉模型 | 正确识别生成图片中的红色方形和蓝色圆形。 |
| AnySearch | 真实返回 3 条结果；微信群模型调用搜索未测。 |
| Embedding | 3 个 1024 维有限向量；相同输入向量一致。 |
| RAG | 数据库完整；1 个知识块、1 个 FAISS 向量；实际嵌入查询取得有效索引。群内引用和资料缺失拒答未测。 |
| LivingMemory | 198 项针对性测试通过，覆盖作用域、捕获、注入、召回、图谱、记忆片段及临时数据库集成。生产记忆和会话表为空，不能视为群内通过。 |
| 表情包小偷 | 分进程运行 90 项测试通过：过滤 4、作用域 18、自动流程 18、队列 9、数据库 36、视觉调用 5。联合运行曾有测试桩冲突。当前图片库和待审核池为空。 |
| 桥接原有回归 | 41 通过、2 失败；失败用例仍断言立即发送，与当前 15 秒聚合不同，未改测试掩盖失败。 |
| 桥接补充测试 | 模拟发送器验证两次请求合为一次、统一格式化、回读检查及失败时移除回声标记。 |
| 群管理规则 | 使用当前配置和临时数据库：第三次重复进入审核；成员/群隔离及关键词群隔离通过。 |
| 内容审核 | 禁词、正则、广告域名均为空；语义审核和输出审核未验证。 |
| Spectre 群白名单 | 被排除群不触发 Spectre；不等于所有插件出站路径验收。 |
| Spectre URL 输入 | 配置中的斜杠黑名单会排除含网址的皮皮请求；原生聊天仍可能继续处理。 |
| 静默决策 | 执行当前 ProcessStage 的隔离测试证明：存在合成 @ 时，即使 Spectre 跳过，原生 Agent 仍调用一次。 |
| 双回复 | 隔离测试中两种路径均仅调用一次 Agent，前述“双回复”猜测没有复现，不能当成已确认故障。 |
| 微信真实文字发送 | 一次发送超时，WeFlow 未找到测试标记；随后日志确认目标标题/输入框缺失，输入前被拦截，未重复发送。 |
| 微信入站到回复、记忆、表情闭环 | 阻塞：没有新的受控群成员输入；机器人账号自己发送不能替代成员入站验收。 |

## 配置清理边界

- 暂不删除 astrbot_plugin_smart_core.json，微信群管理仍读取其中全局/群级开关及审核配置。
- 活跃模型配置已没有 Smart 适配器类型引用。
- 备份文件不会参与运行，删除它们不能修复发送或上游模型故障；保留必要回退点。
- shared_preferences.json 不能单独代表启用状态，实际偏好还存储于 data_v4.db。
- 人格、有效模型、权限白名单、知识库与记忆数据必须保留。本轮未清理。

## 输入框阻塞修复复验

- 现场确认主窗口可找到会话列表，但聊天标题和输入框未出现在 UIA 树中，激活和等待没有恢复。精确选中目标会话后打开独立聊天窗，可读取两项控件。
- 项目 UiaSender 增加受控回退：主窗缺少标题或输入框时，仅在稳定会话映射存在、目标会话项唯一且焦点确认后发送 Enter 打开独立窗；继续验证同进程、精确群名和 ChatSingleWindow 会话 ID。错配或歧义停止，不降低发送保护。
- 运行与发布副本同步修改；新增 4 项回归，相关 UIA 测试共 20 项通过，编译与差异检查通过。
- 项目发送器实发及桥接重启后的运行接口实发均得到 WeFlow 正文回读。运行接口测试消息恰好 1 条，isSend=1，Markdown 转换后的正文逐字匹配。
- 微信文字出站子项现已通过。两个备用模型的上游 502、合成 @ 静默问题及成员入站/记忆/表情完整链路仍保持原测试结论。
- 原测试阶段失败记录保留作为修复前证据；未提交推送，也未清理旧配置。

## 后续真实验收

2026-09-09 群风格学习已部署为后续阶段：复用现有微信群管理插件，在允许且启用的 pipi 群按群采样；累计 30 条有效发言且至少两名成员参与后，调用现有 Gemini Flash 提炼句长、语气、表情频率和多成员共同短语。只持久化短风格结果，样本驻留内存并过滤命令、媒体占位、链接、凭据、长文本和单成员样本；按平台与群 ID 隔离。通过 /wx 风格 查看|开启|暂停|清空 管理，风格只调整表达方式，不改变人格、权限、事实或是否回复。发布副本风格模块单测 3 项、图片适配 2 项、管理员边界 2 项通过；容器编译和重启加载通过。真实 30 条样本提炼和群内效果仍待自然聊天积累，不能提前标 PASS。

2026-09-09 表情阶段为 PARTIAL：真实群入站已携带 image，表情库回读 1 张，随后 search_meme 命中、send_meme 发出，09:52:46 WeFlow 回读一条出站图片。此证据覆盖单张入库及发送，不代表整批收录；模型“全部收录”的回复与库存不符。排查自动流程发现缺少表情类型标记，且插件后台队列要求本地/HTTP 文件。桥接原生表情分支已补 sub_type=1，现有微信群管理在白名单校验后调用 AstrBot convert_to_file_path，为后续插件提供本地文件。未更改冷却或审核设置。另修复连续下载的秒级文件名冲突，独占临时文件写完再返回。运行/发布桥接回归 47/48 项、媒体适配 2 项通过，容器原生 Image 解码验证通过；服务重载、插件加载和文件哈希回读一致，四项连接正常。自动收录仍待重载后的真实成员表情验证，不重放旧消息充当新验收。普通图片素材匹配与连续多图的完整性仍需验证。

2026-09-09 微信粗体残留修复已部署并真实验收：CommonMark 在中文引号邻接场景中将部分双星号保留为 text。出站转换在 text_join 前清理普通文本中的 ASCII/全角双星号，转义 text_special、代码和链接地址保持原值。运行/发布文本桥接测试分别 46/47 项通过；重载后四项连接正常。“记录”群实发原问题句、全角粗体与代码乘方，WeFlow 精确回读一条出站：装饰双星号清除、代码乘方保留。此前记忆回复的排版缺陷已修复，历史消息不会改写。

2026-09-09 09:02 长期记忆召回真实通过：成员请求查询测试盆栽，Agent 实际调用 recall_long_term_memory，返回 count=1、记忆 ID 1，包含小青及每周三浇水，session_filtered/persona_filtered 均为 true。随后一次 send_group_msg，主窗口发送，WeFlow 确认一条相应出站，桥接日志确认送达。此前资料经真实入站、原生命令总结和本次工具召回构成闭环；不代表跨账号隔离及自动满 10 轮总结已全部实测。实际回复残留部分双星号，单列排版缺陷待处理，不影响本次记忆命中结论。

2026-09-09 09:12-09:14 动态热榜真实通过：微博请求先后调用 AnySearch 和 Python 网页抓取，最终采用的 8 个话题均能在同次抓取返回的前 12 项中逐项对应；抖音请求调用 AnySearch 后，再从抖音公开热榜接口取得 10 项实时结果，最终 8 项均能逐项对应。两次请求各产生一次 send_group_msg，均由微信主窗口发送并经 WeFlow 回读确认。直连微博接口曾返回 403，两个第三方聚合接口曾出现 DNS/502，但 Agent 随后换用可用来源完成回复，未把失败结果冒充成功。当前回复没有附来源链接，因此本项只证明动态榜单内容与真实工具结果一致；来源展示仍以前一项官方文档搜索为已通过证据。

权限更新：微信群管理统一使用 AstrBot 原生管理员身份，删除重复管理员配置入口；表情插件的自然语言手动入库工具增加管理员门禁。原生两个管理员 ID 保留。执行级测试验证管理员放行、普通成员/缺失身份/异常身份拒绝，包装工具事件同样拒绝普通成员，拒绝发生在图片访问前。部署回读与启动通过，真实普通成员拒绝流程仍待账号验证。第三方补丁保存在 ops/stealer-admin-only.patch，升级后须检查仍有效；普通查看、聊天、搜索和自动收集原规则保留。

记忆实测发现并修复桥接消息 ID 缺失：测试资料实际进入 AstrBot 并得到回复，但 LivingMemory 只保存助手复述，没有保存用户原文。桥接 make_message_event 未提供 message_id；实测 aiocqhttp 缺字段返回 None，AstrBot 转为字符串 None，LivingMemory 将其作为同一个去重键缓存 300 秒。现为每个桥接事件生成独立且 JavaScript 安全的正整数 ID，同一事件推送保留该 ID；新增群聊/私聊重复正文唯一性回归。运行/发布测试分别 48/49 项通过，桥接重启并连接正常。旧漏收消息未补写；修复后的真实用户原文捕获、原生命令总结和长期记忆工具召回已于 09:02 完成闭环，自动阈值总结与跨账号隔离仍待独立验收。

2026-09-09 08:04-08:05 联网搜索真实验收通过：测试群成员明确请求搜索官方文档，服务端记录两次 web_search_anysearch 调用及结果，第二次取得官方文档域名下的来源。最终回复附文档、官网与代码仓库链接，仅一次 send_group_msg；桥接主窗口发送后 WeFlow 回读确认一条对应出站，未用模型自述代替工具证据。约 35 秒完成从微信入站到出站。期间有一条提及用户信息查询超时告警，未阻断本次业务，保留待排查。

主界面回复更新：按用户要求移除独立窗自动回退，当前目标群已打开时不点击会话，需要切换时仅点击一次。运行接口真实发送后，WeFlow 精确回读一条出站消息，目标群子窗口数为 0；运行/发布回归分别 47/48 项通过。前述独立窗方案仅为历史证据。用户成员账号明确 @ 皮皮的真实应答链路已通过：一条入站、一次 send_group_msg、微信送达回读成功；普通闲聊静默、记忆和表情仍须各自验收。

后续已部署触发修复：普通群消息不再添加合成 @；原文 ASCII/全角昵称提及转换为 At，非批处理正文不再加发言人前缀，命令与原生唤醒词保持在开头，发送者身份仍由 OneBot 字段承载。运行副本 49 项、发布副本 50 项回归通过；本机桥接已重启加载，四项连接状态恢复。真实成员消息尚待验收，不能仅据回归宣称群聊静默通过。

原生备用列表已从两个持续上游 502 的模型改为现有 gemini_aux_source/gemini-3-flash；该模型的文本、强制工具调用和生成图片识别实测通过。其他已有 DeepSeek 候选返回上游禁止访问，因此没有接入备用。原模型定义保留，没有扩大账号权限或新增凭据。AstrBot 重启后主模型、备用配置和 LivingMemory 初始化均回读正常；真实主模型故障下的群内切换尚待验证。

修复发送和触发阻塞后，在既定测试群使用成员账号覆盖明确呼叫、普通闲聊静默、命令、重复消息、搜索来源、RAG 命中与缺失、记忆保留与隔离、表情收集审核及送达。每例关联真实入站及回读，不用启动日志、模型自述或模拟测试替代。
