# astrbot_plugin_wechat_group_manager

> 状态：线上已停用。本目录保留历史实现、静态检查和迁移证据，不代表当前微信主链路仍加载本插件。

面向 Akasha-WeChat / WeFlow 链路的 AstrBot 微信群管理插件。

当前能力包括稳定 ID管理员白名单、群级运行状态、回复模式、静默时段、关键词、软审核队列和操作审计。管理命令以 `/wx` 开头；权限判断使用协议层稳定发送者 ID，显示名只用于展示。


配置文件或数据库保存成功不代表所有运行内存已经刷新。每项配置仍需按“持久化、内存加载、真实业务行为”三层验收。
